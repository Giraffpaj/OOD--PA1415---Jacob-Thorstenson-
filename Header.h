#include <SFML/Graphics.hpp>


int main()
{
	//CreatingGamelogic

	//Creating Data

	//Creating Window
	sf::RenderWindow window(sf::VideoMode(640, 480), "SFML Application");

	while (window.isOpen())
	{

		

		window.clear();
		window.draw(shape);
		window.display();


		






	}
}